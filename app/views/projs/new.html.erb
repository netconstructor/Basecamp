<% form_for(@proj) do |f| %>
  <%= f.error_messages %>
	<p>
		<%= f.label "Project Name"%><br>
		<%= f.text_field :name %><br>
	</p>
  <p>
	<%= f.hidden_field :user_id ,:value=>@user.id %>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', projs_path %>